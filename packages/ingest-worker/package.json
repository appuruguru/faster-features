{
  "name": "faster-features-ingest",
  "version": "0.1.0",
  "private": true,
  "description": "Stateless Cloudflare Worker that turns anonymous app feedback into GitHub issues.",
  "scripts": {
    "setup": "node setup.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
